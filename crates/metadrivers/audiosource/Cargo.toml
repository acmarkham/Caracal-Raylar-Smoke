[package]
name = "raylar-audiosource"
version = "0.1.0"
edition.workspace = true
license.workspace = true

[dependencies]
embassy-sync = "0.7.2"

[features]
default = []
defmt = ["dep:defmt", "embassy-sync/defmt"]

[dependencies.defmt]
workspace = true
optional = true

[dev-dependencies]
critical-section = { version = "1.2.0", features = ["std"] }
